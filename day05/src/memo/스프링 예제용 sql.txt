CREATE USER SPRINGDB IDENTIFIED BY aA123456;

GRANT RESOURCE, CONNECT TO SPRINGDB;

CREATE TABLE MEMBER(
	USERNO NUMBER(10) PRIMARY KEY,
	USERID VARCHAR2(40) NOT NULL UNIQUE,
	USERPW VARCHAR2(65) NOT NULL,
	USERNM VARCHAR2(40) NOT NULL,
	EMAIL VARCHAR2(100),
	REGDT DATE DEFAULT SYSDATE
);

CREATE SEQUENCE SEQ_MEMBER